---
description: Start automated iterative planning with 10-actor parallel verification pipeline (up to 10 iterations)
---

Invoke the `bishx:plan` skill and follow it exactly.

Target task: $ARGUMENTS

Note: The user can request specific skills by adding `+skill:name` to their task description (e.g., `+skill:copywriting +skill:seo-audit`). These skills will be auto-injected into the researcher context.
